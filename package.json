{
  "name": "gest_back",
  "version": "1.0.50",
  "scripts": {
    "migrate": "bunx prisma migrate dev --name init --schema=./prisma/schema",
    "generate": "bunx prisma generate --schema=./prisma/schema",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.1",
    "@elysiajs/jwt": "^1.3.0",
    "@elysiajs/swagger": "^1.3.0",
    "@prisma/client": "^6.7.0",
    "bcrypt": "^5.1.1",
    "elysia": "latest",
    "prisma": "^6.7.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
