enum ROLE {
    MASTER
    ADMIN
    USER
}

enum LICENSE_STATUS {
    ACTIVE
    INACTIVE
    EXPIRED
}

enum EQUIPAMENT_STATUS {
    ACTIVE
    INACTIVE
    MAINTENANCE
}

enum FIELD_TYPES {
    STRING
    NUMBER
    BOOLEAN
    DATE
}

enum REPORT_STATUS {
    FINALIZED
    IN_PROGRESS
}
